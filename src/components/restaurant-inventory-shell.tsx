"use client";

import Link from "next/link";
import { usePathname } from "next/navigation";
import { useState, type ReactNode } from "react";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { Check, ClipboardList, Eye, Plus } from "lucide-react";
import {
  cancelRestaurantReceipt, confirmRestaurantReceipt, createRestaurantConsumption,
  createRestaurantWaste, fetchRestaurantDashboard,
  fetchRestaurantMovements, fetchRestaurantReceipts,
  fetchRestaurantRecipes, fetchRestaurantStock, previewRestaurantConsumption,
  getApiErrorMessage,
} from "@/lib/backend";
import { useAppStore } from "@/store/app-store";
import { ModuleRouteGuard } from "@/components/module-route-guard";
import { AsyncState } from "@/components/async-state";
import { InlineFeedback, PageHeader } from "@/components/design-system";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { RestaurantPhase2View } from "@/components/restaurant-inventory-phase2";
import { RestaurantSalesImport } from "@/components/restaurant-sales-import";
import { RestaurantReportsView } from "@/components/restaurant-reports";
import { RestaurantInventoryCatalog } from "@/components/restaurant-inventory-catalog";
import { RestaurantReceiptsScreen } from "@/components/restaurant-receipts";
import { RestaurantOperations } from "@/components/restaurant-operations";
import { RestaurantRecipesWorkspace } from "@/components/restaurant-recipes-workspace";
import { RestaurantInventoryContextBar, RestaurantInventoryContextProvider, useRestaurantInventoryContext } from "@/components/restaurant-inventory-context";
import { RestaurantStatusBadge } from "@/components/restaurant-inventory-ui";
import type { PermissionKey } from "@/lib/contracts";

type InventoryItem = { key: string; label: string; href: string; permission: PermissionKey };
type InventoryTask = { key: string; label: string; description: string; items: InventoryItem[] };
const taskGroups: InventoryTask[] = [
  { key: "overview", label: "Dashboard", description: "Visión general", items: [{ key: "dashboard", label: "Resumen", href: "/inventory/restaurant", permission: "restaurant_inventory.view" }] },
  { key: "supply", label: "Abastecimiento", description: "Compras y catálogos", items: [
    { key: "receipts", label: "Entradas", href: "/inventory/restaurant/receipts", permission: "restaurant_inventory.manage" }, { key: "ingredients", label: "Ingredientes", href: "/inventory/restaurant/ingredients", permission: "restaurant_inventory.view" }, { key: "suppliers", label: "Proveedores", href: "/inventory/restaurant/suppliers", permission: "restaurant_inventory.view" }, { key: "warehouses", label: "Almacenes", href: "/inventory/restaurant/warehouses", permission: "restaurant_inventory.view" }, { key: "categories", label: "Categorías", href: "/inventory/restaurant/categories", permission: "restaurant_inventory.view" }, { key: "units", label: "Unidades", href: "/inventory/restaurant/units", permission: "restaurant_inventory.view" },
  ] },
  { key: "production", label: "Producción", description: "Recetas y elaboración", items: [{ key: "recipes", label: "Recetas", href: "/inventory/restaurant/recipes", permission: "restaurant_inventory.view" }, { key: "production", label: "Producción", href: "/inventory/restaurant/production", permission: "restaurant_inventory.manage" }] },
  { key: "daily", label: "Operación diaria", description: "Consumo y merma", items: [{ key: "consumption", label: "Consumo", href: "/inventory/restaurant/consumption", permission: "restaurant_inventory.manage" }, { key: "waste", label: "Desperdicios", href: "/inventory/restaurant/waste", permission: "restaurant_inventory.manage" }] },
  { key: "control", label: "Control", description: "Existencias y trazabilidad", items: [{ key: "stock", label: "Existencias", href: "/inventory/restaurant/stock", permission: "restaurant_inventory.view" }, { key: "lots", label: "Lotes y vencimientos", href: "/inventory/restaurant/lots", permission: "restaurant_inventory.view" }, { key: "stock-counts", label: "Conteo físico", href: "/inventory/restaurant/stock-counts", permission: "restaurant_inventory.manage" }, { key: "adjustments", label: "Ajustes", href: "/inventory/restaurant/adjustments", permission: "restaurant_inventory.manage" }, { key: "transfers", label: "Transferencias", href: "/inventory/restaurant/transfers", permission: "restaurant_inventory.manage" }, { key: "movements", label: "Movimientos", href: "/inventory/restaurant/movements", permission: "restaurant_inventory.view" }] },
  { key: "analysis", label: "Análisis", description: "Decisiones y auditoría", items: [{ key: "reports", label: "Reportes", href: "/inventory/restaurant/reports", permission: "restaurant_inventory.view" }, { key: "analytics", label: "Análisis", href: "/inventory/restaurant/analytics", permission: "restaurant_inventory.view" }, { key: "costs", label: "Costos y rentabilidad", href: "/inventory/restaurant/costs", permission: "restaurant_inventory.view" }, { key: "purchase-suggestions", label: "Sugerencias de compra", href: "/inventory/restaurant/purchase-suggestions", permission: "restaurant_inventory.view" }, { key: "audit", label: "Auditoría", href: "/inventory/restaurant/audit", permission: "restaurant_inventory.view" }] },
];

export function RestaurantInventoryShell() {
  return <ModuleRouteGuard module="restaurant_inventory" permission="restaurant_inventory.view"><RestaurantInventoryContextProvider><RestaurantInventoryInner /></RestaurantInventoryContextProvider></ModuleRouteGuard>;
}

function RestaurantInventoryInner() {
  const pathname = usePathname();
  const section = pathname.includes("/recipes") ? "recipes" : taskGroups.flatMap((task) => task.items).find((item) => item.key !== "dashboard" && pathname.endsWith(`/${item.key}`))?.key ?? "dashboard";
  const { currentBranch, can } = useAppStore();
  const { warehouseId, warehouseName } = useRestaurantInventoryContext();
  const visibleTasks = taskGroups.map((task) => ({ ...task, items: task.items.filter((item) => can(item.permission)) })).filter((task) => task.items.length);
  const activeTask = visibleTasks.find((task) => task.items.some((item) => item.key === section))?.key ?? "overview";
  const [selectedTask, setSelectedTask] = useState(activeTask);
  const task = visibleTasks.find((item) => item.key === selectedTask) ?? visibleTasks[0];
  return <div className="space-y-6">
    <PageHeader eyebrow="Operaciones" title="Inventario de restaurante" description="Ingredientes, recetas y movimientos con trazabilidad de backend." />
    <nav className="grid gap-2 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-6" aria-label="Tareas de inventario de restaurante">{visibleTasks.map((item) => <button key={item.key} type="button" onClick={() => setSelectedTask(item.key)} className={`rounded-2xl border px-4 py-3 text-left transition ${selectedTask === item.key ? "border-primary bg-primary/10 shadow-sm" : "border-border-default bg-surface-elevated hover:border-primary/40"}`}><span className="block text-sm font-semibold">{item.label}</span><span className="mt-1 block text-xs text-text-secondary">{item.description}</span></button>)}</nav>
    {task ? <div className="flex flex-wrap gap-2 rounded-2xl border border-border-default bg-surface-elevated p-3" aria-label={`Rutas de ${task.label}`}>{task.items.map((item) => <Button key={item.key} asChild size="sm" variant={section === item.key ? "default" : "secondary"}><Link href={item.href}>{item.label}</Link></Button>)}</div> : null}
    <RestaurantInventoryContextBar />
    <Card level={1}><CardContent className="flex flex-wrap gap-4 p-4 text-sm text-text-secondary"><span>Empresa: contexto actual</span><span>Sucursal: {currentBranch?.name ?? "Sin sucursal"}</span><span>Almacén: {warehouseName}</span><span>Stock y costos: fuente de verdad del backend</span></CardContent></Card>
    {section === "dashboard" ? <DashboardScreen branchId={currentBranch?.id} warehouseId={warehouseId} /> : null}
    {section === "ingredients" ? <RestaurantInventoryCatalog kind="ingredients" /> : null}
    {section === "receipts" ? <RestaurantReceiptsScreen /> : null}
    {section === "recipes" ? <RestaurantRecipesWorkspace /> : null}
    {section === "consumption" ? <RestaurantOperations section="consumption" warehouseId={warehouseId} /> : null}
    {section === "waste" ? <RestaurantOperations section="waste" warehouseId={warehouseId} /> : null}
    {section === "stock" ? <StockScreen branchId={currentBranch?.id} warehouseId={warehouseId} /> : null}
    {section === "movements" ? <MovementsScreen branchId={currentBranch?.id} warehouseId={warehouseId} /> : null}
    {["lots", "stock-counts", "adjustments", "transfers"].includes(section) ? <RestaurantPhase2View section={section} /> : null}
    {section === "production" ? <RestaurantOperations section="production" warehouseId={warehouseId} /> : null}
    {section === "sales-import" ? <RestaurantSalesImport /> : null}
    {["reports", "analytics", "costs", "purchase-suggestions", "audit"].includes(section) ? <RestaurantReportsView section={section} /> : null}
    {["categories", "units", "suppliers", "warehouses"].includes(section) ? <RestaurantInventoryCatalog kind={section as "categories" | "units" | "suppliers" | "warehouses"} /> : null}
  </div>;
}

function QueryState({ loading, error, retry, children }: { loading: boolean; error: unknown; retry: () => void; children: ReactNode }) {
  if (loading) return <AsyncState state="loading" />;
  if (error) return <AsyncState state="error" onRetry={retry} description={getApiErrorMessage(error, "No fue posible consultar el inventario de restaurante.")} />;
  return <>{children}</>;
}

function DashboardScreen({ branchId, warehouseId }: { branchId?: string; warehouseId?: string }) {
  const query = useQuery({ queryKey: ["restaurant-dashboard", branchId, warehouseId], queryFn: () => fetchRestaurantDashboard({ branchId, warehouseId }) });
  const data = query.data;
  return <QueryState loading={query.isLoading} error={query.error} retry={() => void query.refetch()}><div className="space-y-5"><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{[["Valor aproximado", `$${Number(data?.totalValue ?? 0).toFixed(2)}`], ["Ingredientes activos", data?.activeIngredients ?? 0], ["Bajo mínimo", data?.belowMinimum ?? 0]].map(([label, value]) => <Card key={String(label)} level={2}><CardContent className="p-5"><p className="text-sm text-text-secondary">{label}</p><p className="mt-2 text-2xl font-semibold">{value}</p></CardContent></Card>)}</div><RecentList title="Entradas recientes" items={data?.recentReceipts.map((item) => `${item.reference} · ${item.status} · $${item.total.toFixed(2)}`) ?? []} /><RecentList title="Consumos y desperdicios recientes" items={[...(data?.recentConsumption ?? []).map((item) => `Consumo ${item.date} · $${item.totalCost.toFixed(2)} · ${item.status}`), ...(data?.recentWaste ?? []).map((item) => `Desperdicio: ${item.reason} · $${item.totalCost.toFixed(2)}`)]} /></div></QueryState>;
}

function RecentList({ title, items }: { title: string; items: string[] }) { return <Card level={2}><CardContent className="p-5"><h2 className="font-semibold">{title}</h2>{items.length ? <ul className="mt-3 space-y-2 text-sm text-text-secondary">{items.map((item, index) => <li key={`${item}-${index}`} className="border-b border-border-default pb-2">{item}</li>)}</ul> : <p className="mt-3 text-sm text-text-secondary">No hay registros para este contexto.</p>}</CardContent></Card>; }

export function ReceiptsScreen({ branchId }: { branchId?: string }) { const qc = useQueryClient(); const query = useQuery({ queryKey: ["restaurant-receipts", branchId], queryFn: () => fetchRestaurantReceipts({ branchId }) }); const action = useMutation({ mutationFn: ({ id, type }: { id: string; type: "confirm" | "cancel" }) => type === "confirm" ? confirmRestaurantReceipt(id) : cancelRestaurantReceipt(id, "Cancelación solicitada por el usuario"), onSuccess: () => void qc.invalidateQueries({ queryKey: ["restaurant-receipts"] }) }); return <div className="space-y-4"><PageHeader eyebrow="Abastecimiento" title="Entradas de mercancía" description="Confirma únicamente después de revisar el resumen. Un documento confirmado no puede editarse." actions={<Button><Plus className="size-4" />Nueva entrada</Button>} /><QueryState loading={query.isLoading} error={query.error} retry={() => void query.refetch()}><ResponsiveTable headers={["Referencia", "Fecha", "Proveedor", "Total", "Estado", "Acciones"]}>{(query.data ?? []).map((item) => <tr key={item.id}><td>{item.reference}</td><td>{item.date}</td><td>{item.supplierName ?? "Sin proveedor"}</td><td>${item.total.toFixed(2)}</td><td><RestaurantStatusBadge status={item.status} /></td><td>{item.status === "DRAFT" ? <span className="flex flex-wrap gap-2"><Button size="sm" onClick={() => { if (window.confirm("¿Confirmar esta entrada? El documento no podrá editarse después.")) action.mutate({ id: item.id, type: "confirm" }); }}><Check className="size-4" />Confirmar</Button><Button size="sm" variant="secondary" onClick={() => action.mutate({ id: item.id, type: "cancel" })}>Cancelar</Button></span> : <Button size="sm" variant="ghost"><Eye className="size-4" />Consultar</Button>}</td></tr>)}</ResponsiveTable></QueryState></div>; }

export function RecipesScreen() { const query = useQuery({ queryKey: ["restaurant-recipes"], queryFn: () => fetchRestaurantRecipes() }); return <div className="space-y-4"><PageHeader eyebrow="Producción" title="Recetas" description="El costo total, costo por porción y margen estimado son calculados por el backend." actions={<Button><Plus className="size-4" />Nueva receta</Button>} /><QueryState loading={query.isLoading} error={query.error} retry={() => void query.refetch()}><div className="grid gap-4 lg:grid-cols-2">{(query.data ?? []).map((recipe) => <Card key={recipe.id} level={2}><CardContent className="space-y-3 p-5"><div className="flex justify-between gap-3"><div><p className="text-sm text-text-secondary">{recipe.code}</p><h2 className="font-semibold">{recipe.name}</h2></div><Badge>{recipe.status}</Badge></div><p className="text-sm text-text-secondary">{recipe.lines.length} ingredientes · rendimiento {recipe.yieldQuantity}</p><div className="grid grid-cols-3 gap-2 text-sm"><span>Costo<br /><strong>${recipe.totalCost.toFixed(2)}</strong></span><span>Porción<br /><strong>${recipe.costPerPortion.toFixed(2)}</strong></span><span>Margen<br /><strong>{recipe.estimatedMargin ?? "-"}%</strong></span></div></CardContent></Card>)}</div></QueryState></div>; }

export function ConsumptionScreen({ branchId }: { branchId?: string }) { const [recipeId, setRecipeId] = useState(""); const [quantity, setQuantity] = useState("1"); const recipes = useQuery({ queryKey: ["restaurant-recipes"], queryFn: () => fetchRestaurantRecipes() }); const preview = useMutation({ mutationFn: () => previewRestaurantConsumption({ branchId, lines: [{ recipeId, quantity: Number(quantity) }] }) }); const confirm = useMutation({ mutationFn: () => createRestaurantConsumption({ branchId, lines: [{ recipeId, quantity: Number(quantity) }], previewToken: (preview.data as { previewToken?: string } | undefined)?.previewToken }), onSuccess: () => preview.reset() }); return <div className="space-y-4"><PageHeader eyebrow="Operación" title="Registro de consumo" description="Calcula el consumo con la vista previa del backend antes de confirmar." /><Card level={2}><CardContent className="grid gap-3 p-5 sm:grid-cols-[1fr_160px_auto] sm:items-end"><div><Label htmlFor="consumption-recipe">Receta</Label><select id="consumption-recipe" className="h-11 w-full rounded-2xl border border-border-default bg-surface-elevated px-3" value={recipeId} onChange={(event) => setRecipeId(event.target.value)}><option value="">Seleccionar receta</option>{(recipes.data ?? []).map((recipe) => <option key={recipe.id} value={recipe.id}>{recipe.name}</option>)}</select></div><div><Label htmlFor="consumption-quantity">Cantidad vendida</Label><Input id="consumption-quantity" type="number" min="1" value={quantity} onChange={(event) => setQuantity(event.target.value)} /></div><Button disabled={!recipeId || preview.isPending} onClick={() => preview.mutate()}>{preview.isPending ? "Calculando…" : "Calcular consumo"}</Button></CardContent></Card>{preview.error ? <InlineFeedback tone="danger" title="No se pudo calcular">{getApiErrorMessage(preview.error, "El backend no pudo generar la vista previa.")}</InlineFeedback> : null}{preview.data ? <Card level={2}><CardContent className="space-y-4 p-5"><h2 className="font-semibold">Vista previa del consumo</h2><ResponsiveTable headers={["Ingrediente", "Existencia actual", "Requerido", "Resultante", "Disponibilidad"]}>{preview.data.ingredients.map((item) => <tr key={item.ingredientId}><td>{item.ingredientName}</td><td>{item.currentStock} {item.unit}</td><td>{item.requiredQuantity} {item.unit}</td><td>{item.resultingStock} {item.unit}</td><td><Badge variant={item.sufficient ? "default" : "destructive"}>{item.sufficient ? "Disponible" : "Insuficiente"}</Badge></td></tr>)}</ResponsiveTable>{preview.data.insufficientIngredients.length ? <InlineFeedback tone="warning" title="Inventario insuficiente">{preview.data.insufficientIngredients.join(", ")}</InlineFeedback> : null}<p className="font-semibold">Costo total: ${preview.data.totalCost.toFixed(2)}</p><Button disabled={Boolean(preview.data.insufficientIngredients.length) || confirm.isPending} onClick={() => confirm.mutate()}><ClipboardList className="size-4" />Confirmar consumo</Button></CardContent></Card> : null}</div>; }

export function WasteScreen({ branchId }: { branchId?: string }) { const mutation = useMutation({ mutationFn: (input: Record<string, unknown>) => createRestaurantWaste(input) }); const [reason, setReason] = useState(""); const [ingredientId, setIngredientId] = useState(""); const [quantity, setQuantity] = useState(""); return <div className="space-y-4"><PageHeader eyebrow="Control" title="Desperdicios" description="Registra la merma con motivo, cantidad, unidad y observaciones." /><Card level={2}><CardContent className="grid gap-3 p-5 sm:grid-cols-2"><div><Label htmlFor="waste-reason">Motivo</Label><Input id="waste-reason" value={reason} onChange={(event) => setReason(event.target.value)} required /></div><div><Label htmlFor="waste-ingredient">Ingrediente</Label><Input id="waste-ingredient" value={ingredientId} onChange={(event) => setIngredientId(event.target.value)} placeholder="ID del ingrediente" required /></div><div><Label htmlFor="waste-quantity">Cantidad</Label><Input id="waste-quantity" type="number" min="0" value={quantity} onChange={(event) => setQuantity(event.target.value)} required /></div><div className="sm:col-span-2"><Button disabled={mutation.isPending || !reason || !ingredientId || !quantity} onClick={() => mutation.mutate({ branchId, reason, lines: [{ ingredientId, quantity: Number(quantity) }] })}>{mutation.isPending ? "Registrando…" : "Registrar desperdicio"}</Button></div></CardContent></Card>{mutation.error ? <InlineFeedback tone="danger" title="No se pudo registrar">{getApiErrorMessage(mutation.error, "Revisa la información.")}</InlineFeedback> : null}{mutation.isSuccess ? <InlineFeedback tone="success" title="Desperdicio registrado">El backend confirmó el movimiento.</InlineFeedback> : null}</div>; }

function StockScreen({ branchId, warehouseId }: { branchId?: string; warehouseId?: string }) { const query = useQuery({ queryKey: ["restaurant-stock", branchId, warehouseId], queryFn: () => fetchRestaurantStock({ branchId, warehouseId }) }); return <div className="space-y-4"><PageHeader eyebrow="Control" title="Existencias" description="Consulta existencias, mínimos, costo promedio y valor total." /><QueryState loading={query.isLoading} error={query.error} retry={() => void query.refetch()}><ResponsiveTable headers={["Ingrediente", "Sucursal", "Almacén", "Existencia", "Unidad", "Mínimo", "Costo promedio", "Estado"]}>{(query.data ?? []).map((item) => <tr key={item.id}><td>{item.name}</td><td>Contexto actual</td><td>{warehouseId ? "Almacén activo" : "Todos"}</td><td>{item.stock}</td><td>{item.inventoryUnit}</td><td>{item.minimumStock}</td><td>${item.averageCost.toFixed(2)}</td><td><Badge variant={item.stock < item.minimumStock ? "destructive" : "default"}>{item.stock < item.minimumStock ? "BAJO MÍNIMO" : item.status}</Badge></td></tr>)}</ResponsiveTable></QueryState></div>; }

function MovementsScreen({ branchId, warehouseId }: { branchId?: string; warehouseId?: string }) { const query = useQuery({ queryKey: ["restaurant-movements", branchId, warehouseId], queryFn: () => fetchRestaurantMovements({ branchId, warehouseId }) }); return <div className="space-y-4"><PageHeader eyebrow="Auditoría" title="Movimientos" description="Consulta de solo lectura con documento de referencia y usuario." /><QueryState loading={query.isLoading} error={query.error} retry={() => void query.refetch()}><ResponsiveTable headers={["Fecha", "Ingrediente", "Tipo", "Entrada", "Salida", "Saldo", "Costo", "Referencia", "Usuario"]}>{(query.data ?? []).map((item) => <tr key={item.id}><td>{item.date}</td><td>{item.ingredientName}</td><td>{item.type}</td><td>{item.entry}</td><td>{item.exit}</td><td>{item.balance}</td><td>${item.cost.toFixed(2)}</td><td>{item.reference ?? "-"}</td><td>{item.userName ?? "-"}</td></tr>)}</ResponsiveTable></QueryState></div>; }

function ResponsiveTable({ headers, children }: { headers: string[]; children: ReactNode }) { return <div className="overflow-x-auto rounded-2xl border border-border-default"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-surface-interactive text-text-secondary"><tr>{headers.map((header) => <th key={header} className="px-4 py-3 font-medium">{header}</th>)}</tr></thead><tbody className="divide-y divide-border-default">{children}</tbody></table></div>; }
